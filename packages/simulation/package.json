{
  "name": "@game/simulation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "TMPDIR=/tmp TEMP=/tmp TMP=/tmp vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@game/config": "workspace:*",
    "@game/protocol": "workspace:*"
  }
}
